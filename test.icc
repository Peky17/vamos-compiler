// Simular importar paquete fmt
importar "fmt"

variable num entero
variable valDec decimal
constante MAX entero=10
variable i, j entero
variable nom alfabetico="Juan"

funcion ejemplo(a entero, b entero){
  fmt.Imprimenl("Funcion 1")
}

funcion ejemploLlamada(){
  fmt.Imprimenl("Llamada sin params")
}

// Funcion principal
funcion principal() {
  // Llamar funcion sin parametros
  ejemploLlamada()
  // Llamar funcion con parametros
  ejemplo(i, num)
  // asignar valor
  k = 5
}