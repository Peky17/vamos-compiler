// Simular importar paquete fmt
importar "fmt"

variable num entero
variable nom alfabetico="Juan"

// Funcion principal
funcion principal() {
  fmt.Imprimenl("Hola Mundo " , "Bienvenidos a VAMOS")
  // Llamar funcion
  //sumaNumeros(num)
}

funcion testSegun(){
  // probar con tipo entero
  segun num {
    caso 1:
      fmt.Imprimenl("El numero es 1")
    interrumpe
    caso 2:
      fmt.Imprimenl("El numero es 2")
    interrumpe
    predeterminado:
      fmt.Imprimenl("El numero es 2")
  }
}

funcion alguna() {
  fmt.Imprimenl("Hola perrillos")
}